# =============================================================================
# SpotifyToYoutubeMusic - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Spotify Scraper Configuration
# -----------------------------------------------------------------------------
# URL settings (usually don't need to change these)
SPOTIFY_URL=https://open.spotify.com/
LIKED_SONGS_URL=https://open.spotify.com/collection/tracks

# Time to wait for user to login (in seconds)
LOGIN_WAIT_TIME=120

# Output CSV filename for scraped songs
CSV_FILENAME=spotify_liked_songs.csv

# Number of songs to save per batch
BATCH_SIZE=10

# Scroll settings (adjust for speed vs stability tradeoff)
SCROLL_PAUSE_TIME=0.3
SCROLL_AMOUNT=-80
SCROLL_SPEED_MULTIPLIER=1.0

# -----------------------------------------------------------------------------
# YouTube Music Uploader Configuration
# -----------------------------------------------------------------------------
# Authentication file path (created automatically on first run)
AUTH_FILE=headers_auth.json

# Delay between API calls to avoid rate limiting (in seconds)
DELAY_BETWEEN_SONGS=0.5

# Start processing from this song index (1-indexed)
START_INDEX=1

# -----------------------------------------------------------------------------
# YouTube Music Authentication (Cookie-based)
# -----------------------------------------------------------------------------
# These are extracted from your browser's YouTube Music session.
# You can either:
#   1. Let the script prompt you interactively (recommended for first time)
#   2. Paste your cookie value here for automated runs
#
# To get your cookie:
#   1. Open https://music.youtube.com in your browser
#   2. Open Developer Tools (F12) > Network tab
#   3. Refresh the page
#   4. Click any request to music.youtube.com
#   5. Copy the "cookie" value from Request Headers

# Your YouTube Music cookie (optional - only needed for non-interactive runs)
# YOUTUBE_MUSIC_COOKIE=your_cookie_value_here
